package tests

import (
	_ "server/models"
	"testing"

	"github.com/stretchr/testify/assert"
)

// TODO:  Add documentation (func TestCreateTimeSlots)
func TestCreateTimeSlots(t *testing.T) {
	assert.True(t, true)
}

// TODO:  Add documentation (func TestGetTimeSlots)
func TestGetTimeSlots(t *testing.T) {
	assert.True(t, true)
}

// TODO:  Add documentation (func TestUpdateTimeSlots)
func TestUpdateTimeSlots(t *testing.T) {
	assert.True(t, true)
}

// TODO:  Add documentation (func TestDeleteTimeSlots)
func TestDeleteTimeSlots(t *testing.T) {
	assert.True(t, true)
}
